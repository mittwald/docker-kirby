<?php

/**
 * Default configuration of the mittwald/kirby image.
 *
 * Everything here is derived from environment variables. Replace this file in
 * your own image to add project options — keep the `require` so that the
 * KIRBY_* variables documented in the README keep working:
 *
 *     <?php
 *     return array_replace_recursive(
 *         require '/usr/local/share/kirby/env-options.php',
 *         [
 *             'debug' => false,
 *         ]
 *     );
 */

return require '/usr/local/share/kirby/env-options.php';
