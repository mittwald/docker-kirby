{
  "name": "mittwald/kirby-docker-app",
  "description": "Base project skeleton used by the mittwald/kirby container images. getkirby/cms is added at build time so that the exact version is pinned by the build matrix.",
  "type": "project",
  "license": "MIT",
  "config": {
    "allow-plugins": {
      "getkirby/composer-installer": true
    },
    "optimize-autoloader": true,
    "sort-packages": true
  },
  "extra": {
    "kirby-cms-path": "kirby"
  }
}
