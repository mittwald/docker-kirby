name: build

# Reusable build pipeline. Everything that varies between images comes from
# versions.json via scripts/resolve-versions.py, so neither this workflow nor
# the Dockerfile has to change when a Kirby branch is added.

on:
  workflow_call:
    inputs:
      push:
        description: Push the resulting images to Docker Hub.
        type: boolean
        default: false
      no-cache:
        description: Ignore the layer cache. Used by the scheduled rebuild so
          that distribution security updates are actually picked up.
        type: boolean
        default: false
    secrets:
      DOCKERHUB_USERNAME:
        required: false
      DOCKERHUB_TOKEN:
        required: false

permissions:
  contents: read

jobs:
  resolve:
    name: resolve build matrix
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.resolve.outputs.matrix }}
    steps:
      - uses: actions/checkout@v4

      - name: Resolve versions.json against Packagist
        id: resolve
        run: |
          {
            echo '### Build matrix'
            echo '```'
            python3 scripts/resolve-versions.py --github-output
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

  build:
    name: kirby ${{ matrix.branch }}
    needs: resolve
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(needs.resolve.outputs.matrix) }}
    steps:
      - uses: actions/checkout@v4

      # Only the multi-architecture build needs emulation, and that one only
      # runs when the result is actually pushed.
      - uses: docker/setup-qemu-action@v3
        if: inputs.push
      - uses: docker/setup-buildx-action@v3

      - name: Build metadata
        id: meta
        run: echo "date=$(date -u +'%Y-%m-%dT%H:%M:%SZ')" >> "$GITHUB_OUTPUT"

      # Single-architecture build that can be loaded into the local daemon, so
      # the smoke test runs against the exact image that is about to be pushed.
      - name: Build for testing
        uses: docker/build-push-action@v6
        with:
          context: ./image
          file: ./image/Dockerfile
          platforms: linux/amd64
          load: true
          no-cache: ${{ inputs.no-cache }}
          tags: kirby-ci:${{ matrix.branch }}
          build-args: |
            BASE_IMAGE=${{ matrix.base_image }}
            KIRBY_VERSION=${{ matrix.kirby_version }}
            PLAINKIT_CONSTRAINT=${{ matrix.plainkit }}
            PHP_VERSION=${{ matrix.php }}
            FRANKENPHP_VERSION=${{ matrix.frankenphp }}
            VCS_REF=${{ github.sha }}
            BUILD_DATE=${{ steps.meta.outputs.date }}
          cache-from: type=gha,scope=kirby-${{ matrix.branch }}
          cache-to: type=gha,mode=max,scope=kirby-${{ matrix.branch }}

      - name: Smoke test
        env:
          BRANCH: ${{ matrix.branch }}
          KIRBY_VERSION: ${{ matrix.kirby_version }}
          PHP_VERSION: ${{ matrix.php }}
        run: |
          ./scripts/smoke-test.sh "kirby-ci:${BRANCH}" \
            --kirby-version "${KIRBY_VERSION}" \
            --php-version "${PHP_VERSION}"

      - name: Log in to Docker Hub
        if: inputs.push
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # Reuses the layers the test build just produced, so only the additional
      # architectures are actually compiled here.
      #
      # Skipped entirely unless the image is pushed. The foreign architecture
      # is built under qemu, which takes several times longer than the whole
      # native build, and on a pull request nothing consumed the result: it is
      # never loaded, and the smoke test runs against the native test image
      # above. What this gives up is that an arm64-only break now surfaces on
      # main instead of in review. It cannot ship one — build and push are the
      # same step, so a failed architecture pushes no image at all — but it
      # does leave main red until someone fixes it.
      - name: Build and push
        if: inputs.push
        uses: docker/build-push-action@v6
        with:
          context: ./image
          file: ./image/Dockerfile
          platforms: ${{ matrix.platforms }}
          push: ${{ inputs.push }}
          no-cache: ${{ inputs.no-cache }}
          provenance: mode=max
          sbom: true
          tags: ${{ join(matrix.tags, ',') }}
          build-args: |
            BASE_IMAGE=${{ matrix.base_image }}
            KIRBY_VERSION=${{ matrix.kirby_version }}
            PLAINKIT_CONSTRAINT=${{ matrix.plainkit }}
            PHP_VERSION=${{ matrix.php }}
            FRANKENPHP_VERSION=${{ matrix.frankenphp }}
            VCS_REF=${{ github.sha }}
            BUILD_DATE=${{ steps.meta.outputs.date }}
          cache-from: type=gha,scope=kirby-${{ matrix.branch }}
          # The test build above only ever exports amd64, so without this the
          # foreign architecture starts from nothing on every run and compiles
          # the PHP extensions under qemu again. Same scope on purpose: what
          # this exports is a superset of what the test build did.
          #
          # `no-cache` above is what keeps the nightly rebuild honest now that
          # these layers are kept. Until now it was set on the test build only,
          # and the foreign architecture came out fresh by accident — because
          # nothing had ever cached it.
          cache-to: type=gha,mode=max,scope=kirby-${{ matrix.branch }}

      # Matrix values go through the environment rather than into the script
      # body, so nothing in versions.json can end up being executed.
      - name: Summary
        env:
          BRANCH: ${{ matrix.branch }}
          KIRBY_VERSION: ${{ matrix.kirby_version }}
          PHP_VERSION: ${{ matrix.php }}
          BASE_IMAGE: ${{ matrix.base_image }}
          PLATFORMS: ${{ inputs.push && matrix.platforms || 'linux/amd64' }}
          PUSHED: ${{ inputs.push }}
          TAGS: ${{ join(matrix.tags, ' ') }}
        run: |
          {
            echo "### kirby ${BRANCH}"
            echo
            echo "- Kirby \`${KIRBY_VERSION}\` on PHP \`${PHP_VERSION}\` (\`${BASE_IMAGE}\`)"
            echo "- Platforms built: \`${PLATFORMS}\`"
            echo "- Pushed: \`${PUSHED}\`"
            echo "- Tags:"
            for tag in ${TAGS}; do echo "  - \`${tag}\`"; done
          } >> "$GITHUB_STEP_SUMMARY"
